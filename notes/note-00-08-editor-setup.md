## Key Take Aways

1. Setting up editor and coding environment correctly saves you enough time later. For example if you are working with python, you have to install python, jupyter, pylance (inline-errors, type-checking, code completion using Language Server Protocol), gitlens (to be aware of git history), black (for code formatting), ruff (linter: which means correcting code without running it, finding bugs, bad coding styles)
